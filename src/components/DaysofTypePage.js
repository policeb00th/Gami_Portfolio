import React from 'react';
import ReactDOM from 'react-dom';
import Header from './Header';

const DaysOfType = () =>(
    <div>
    <Header></Header>
    <p>36 Days of type page</p>
    </div>
)
export default DaysOfType